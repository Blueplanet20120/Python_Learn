# Flask 網站開發 - Heroku 雲端主機教學
#初始化git环境 只需要刚开始做一次
# $ cd my-project/
# $ git init
# $ heroku git:remote -a python-perfume

# Deploy your application 每次修改后重新部署都要执行这个三个命令


# $ git add .
# $ git commit -am "First is app"
# $ git push heroku master

#第一个app.py的heroku的网址：
# https://python-perfume.herokuapp.com/ deployed to Heroku
